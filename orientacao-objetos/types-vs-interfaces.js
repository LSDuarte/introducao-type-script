var animal = {
    hungry: false,
    weaned: true,
    sex: 'male',
    age: 6,
    name: 'Sei la'
};
var greet = function (name) { return 'Hello, ' + name; };
var greett = function (name) { return 'Hello, ' + name; };
